@* This partial is intentionally minimal.
   Layout already includes custom client-side validation (wwwroot/js/form-validation.js).
   Keeping this file prevents missing-partial errors where pages reference it. *@

